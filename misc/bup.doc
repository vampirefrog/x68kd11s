
                      バイナリー差分抽出プログラム

                        『ＢＵＰ   Ver1.1 Rel.3』
                            Thu Sep 19, 1991     

                              取扱説明書

１ このプログラムの目的 -------------------------------------------------------


  BDIF.Xにより作成された差分情報（拡張子がBFDのファイル）
  よりファイルを更新します。


２ 使用方法 -------------------------------------------------------------------

基本的には、

  BUP diff_file

とすることで、 差分ファイル内部に記述された情報を元に対象ファイルを更新します。

(例)
diff.bfdという差分ファイルの場合には、

  BUP diff.bfd

なお、拡張子は省略可能です。

３ オプション -----------------------------------------------------------------

・ -R
   リバースパッチを当てます
   逆差分情報が記録されている場合のみ有効です。

・ -L
   差分ファイルに記録されている差分情報を表示します。

・ -NT
   ファイルチェックの際にタイムスタンプのチェックをしません。

・ -I
   アップデートされるファイルのあるディレクトリを指定します。
   無視定時はカレントディレクトリとなります。

・ -O
   アップデートしたファイルを書き出すディレクトリを指定します。
   無視定時はカレントディレクトリとなります。

４ その他 ---------------------------------------------------------------------

    1. 著作権

      著作権等は、製作者である東出浩一に帰属します。
      ただし、本プログラムはフリーソフトウェアとし、
      いかなる媒体を用いた複製も、その制限を加えません。

    2. 無保証

      本プログラムは正しい動作を望まれて製作されていますが、
      その動作については一切保証されません。
      また、このプログラムによって生じた、いかなる損害についても、
      作者、及び配布者は一切関知いたしません。個人の責任において使用して下さい。

    3. 配布規定

      再配布、転載、改変等すべて自由です。
      ただし転載後にはバージョンアップ時のサポートを必ず行なうようにして下さい。

５ 謝辞 -----------------------------------------------------------------------

本プログラムの開発にあたり以下のプログラムを使わせていただきました。
作者、移植者の方々に感謝致します（敬称略）。 

       μEmacs 3.10-J1.43                 lika/SALT/PEACE/SHUNA/rima
       GNUmake v3.56                      homy
       GCC X6_08(Based on 1.40)           Mariko
       has v2.33                          Y.Nakamura
       hlk v2.12                          SALT
       Gnulib.a v1.42                     T.Ohtsuki
       XCv1付属Library(改)                SHARP/Hudson
       myu header eta version             myu project

６ 連絡先 ---------------------------------------------------------------------

本プログラムは以下のＢＢＳでのみ直接サポートします。

                 MAXBBS ( 03-3474-7237 ) id.num #10 ひがしで
